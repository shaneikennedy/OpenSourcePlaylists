name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Python unit tests
      uses: docker://python:3.6-alpine-3.7
      run: |
        pip install -r requirements.txt
        python -m unittest
